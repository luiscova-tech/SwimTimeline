services:
  - type: web
    name: swimtimeline
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python webapp/server.py --host 0.0.0.0
    autoDeploy: true
